<section class="orders-show">

  <header class="page-header">
    <h1>Order #<%= @order.id %></h1>
  </header>

  <div class="order-summary jumbotron">
    <h4>Thank you for your order! An invoice has been sent to <%= @order.email %></h4>
    <h2>Order Details</h2>
    <div class="panel panel-default items">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th colspan="2">Product</th>
          <th>Unit Price</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
<<<<<<< HEAD
      <% LineItem.where(order_id:@order).each do |item| %>
        <%  Product.where(id:item.product_id).each do |prod| %>
        <%= render 'item', product:prod, quantity:item.quantity.to_i %>
        <% end %>
=======
        <% LineItem.where(order_id:@order).each do |item| %>
          <% Product.where(id:item.product_id).each do |prod| %>
          <%= render 'item', product: prod, quantity: item.quantity.to_i %>
          <% end %>
>>>>>>> feature/order-receipt
        <% end %>
      </tbody>
      <tfoot>
        <tr>
          <th colspan="4">
            TOTAL:
          </th>
          <th>
            <%= @order.total_cents / 100.0 %>
          </th>
        </tr>
      </tfoot>
    </table>
  </div>
  </div>

  <%= link_to "Continue Shopping", root_path, class: 'btn btn-lg btn-primary' %>

</section>
